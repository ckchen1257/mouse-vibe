services:
  mouse-vibe-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: mouse-vibe-server:dev
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
